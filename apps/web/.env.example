# Base URL the browser uses for API calls (proxied by the rewrite in next.config.ts)
NEXT_PUBLIC_API_URL=/api
# Origin the /api/* rewrite forwards to
API_ORIGIN=http://localhost:4000
NEXT_PUBLIC_SOCKET_IO_URL=http://localhost:4000
