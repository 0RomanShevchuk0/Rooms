# Frontend TODO

Отобрано по одному критерию: **что испортит впечатление человеку, которому дали потыкать приложение на час.** Всё, что тестер за это время не почувствует (тёмная тема, фавикон, производительность Konva, мёртвый код, prettier, CI, структура сторов), сюда намеренно не попало — это долг, но не то, обо что спотыкаются.

## Сломается в процессе

- [x] **Ссылку на комнату кинуть нельзя.** Незалогиненный открывает `/rooms/xxx` → `RequireAuth` кидает на логин без `?next=`, а после входа `useAuth` всегда делает `replace(ROUTES.home)`. Человек логинится и оказывается на пустой главной без комнаты.
      **Сделано:** `src/shared/lib/next-path.ts` (санитайзер + сборка `?next=`), `RequireAuth` кладёт туда текущий путь и делает `replace` вместо `push`, `RequireGuest` и `useAuth` его читают, `AuthForm` протаскивает через ссылку «Sign in ↔ Create account». В `InviteRoomDropdown` добавлен пункт «Copy invite link» (`/rooms/<id>?invite=<id>`), а `useRoomInvite` вступает в комнату на 403 — но только при этом параметре: по голой ссылке посторонний по-прежнему получает «не найдено». У того, кто уже участник, параметр вычищается из адресной строки.
  - [ ] **Параметр `invite` подделывается** — его достаточно дописать руками, и он не отзывается. Настоящий контроль доступа — это модель `RoomInvite` с токеном и проверкой при вступлении на API.
   - [ ] **`next` теряется при входе через OAuth.** `startOAuth` уходит на `/api/auth/google`, и куда вернуться после колбэка решает API. Чтобы протащить — нужен `state` или `returnTo`-кука на бэке; фронтом в одиночку не чинится.

- [x] **Любая заминка бэка показывается как «Room not found».** Страница рендерила 404 на любой пустой `room`, а глобальный `retry: false` не давал ни одной повторной попытки. На холодном контейнере 1 ГБ VM первый запрос легко отваливается по таймауту.
      **Сделано:** `src/shared/react-query/retry.ts` — повтор только для сетевых сбоев и 5xx (4xx это ответ, а не сбой), подключён дефолтом в `QueryProvider`. `useRoomByIdQuery` отдаёт `error`, `isFetching` и `refetch`; страница комнаты показывает «не найдено» только на 403/404, на остальном — новый `src/shared/ui/error-screen.tsx` с кнопкой «Try again».

- [ ] **Разлогин посреди игры.** Сокет-сторы ставят `auth.token` один раз при коннекте (`src/shared/lib/realtime/stores/chat-socket.ts:44-52` и два клона). После авто-реконнекта уходит протухший access-token → `connect_error: Unauthorized` → `clearSession()`. Чем дольше открыта вкладка, тем вероятнее; играют долго.
      **Фикс:** на `Unauthorized` сначала refresh через api-мост, обновить `socket.auth.token`, переподключиться; сдаваться только если refresh упал. Повод свести три копипаст-стора в `createSocketStore(namespace)`. _~1 ч_

- [ ] **Сообщения в чате не приходят, если открыта вкладка Info** — а она открыта по умолчанию. `src/widgets/room-sidebar/ui/RoomSidebar.tsx:59-72` размонтирует `<Chat>` вместе с сокет-подпиской.
      **Фикс:** поднять `useMessages` + `useMessagesSocket` на уровень комнаты, вкладки прятать через `hidden`, добавить badge с непрочитанными. _~1.5 ч_

- [ ] **С телефона играть нельзя.** Канвас жёстко `500×500` (`src/features/snake-game/model/useSnakeGame.ts:88`) → горизонтальный скролл и обрезанное поле на экранах уже 500px. Управление только с клавиатуры — на телефоне змейка вообще не едет.
      **Фикс:** размер канваса от ширины контейнера через `ResizeObserver`. Свайпы — отдельной задачей; если не делаем, честно писать «нужен десктоп». _~1 ч_

- [ ] **Обрыв связи выглядит как зависание.** Флаг `connected` в трёх сокет-сторах не читает никто — игра просто замирает без объяснений.
      **Фикс:** баннер «Соединение потеряно, переподключаемся» поверх комнаты по `connected`. _~30 мин_

## Заметят и спросят «это баг?»

- [ ] **Фейковые данные в карточках комнат.** `src/features/room-card/ui/RoomCard.tsx:16-38`: бейдж «Starts in 00:08» и хардкодный статус «Ready» у всех участников. Это первое, что видно после логина, и это неправда — проще убрать, чем объяснять.

- [ ] **У нового пользователя главная пустая.** `src/widgets/rooms-list/ui/RoomsList.tsx:18` рендерит пустой грид без текста — непонятно, загрузка это или поломка. Нужна строчка «Пока нет комнат — создай первую».

- [ ] **В чате не отличить свои сообщения от чужих.** Все одинаковые, слева, без группировки по отправителю и без разделителей по дням. В переписке на двоих мешает сразу.

- [ ] **Первый экран — пустой спиннер на весь вьюпорт.** `src/app/_providers/SessionProvider.tsx:12-14` блокирует всё дерево на время refresh, включая страницы `/auth/*`, где refresh не нужен вовсе.
      **Фикс:** не блокировать `/auth/*`. _~20 мин_

## Порядок

1. Пункты 1, 2, 8, 10 — примерно два часа суммарно, закрывают весь путь «получил ссылку → вошёл → нашёл комнату».
2. Пункты 3, 4, 6 — реалтайм: токен сокета, непрочитанные, индикация обрыва.
3. Пункты 5, 7, 9 — адаптив, фейковые данные, вид чата.
